<h1>Categories</h1>

<% @user_categories.each do |category| %>
  <div>
    <%= link_to category.name, category_decks_path(category) %>
  </div>
<% end %>

<%= link_to "Add Category", new_category_path %>